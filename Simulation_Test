include("Simulation"); // Penser à inclure le bon fichier sur LW ...

var idLeek_01 = DefaultLeekCreation(306, 12, 10, null, [47], []);
var myID = DefaultMyLeekCreation (400, 12, 10, null, [], []);

StartOperation(); 
var obstacles = getObstacles();
debug ("getObstacles() -> " + obstacles + " en " + (EndOperation() - 3) + " opérations.");

StartOperation(); debug ("getCellContent(1) (CELL_EMPTY) -> " + (getCellContent(0) === CELL_EMPTY) + " en " + (EndOperation() - 3) + " opérations.");
StartOperation(); debug ("getCellContent(obstacles[0]) (CELL_OBSTACLE) -> " + (getCellContent(obstacles[0]) === CELL_OBSTACLE) + " en " + (EndOperation() - 8) + " opérations.");
StartOperation(); debug ("getCellContent(306) (CELL_PLAYER) -> " + (getCellContent(306) === CELL_PLAYER) + " en " + (EndOperation() - 3) + " opérations.");